# Copilot Instructions

## プロジェクト ガイドライン
- User prefers the coding standard: declare functions in headers and define them in .cpp files using qualified names (avoid defining free helper functions directly in namespace/anonymous namespace in .cpp where possible).