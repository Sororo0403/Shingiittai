name: Release

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Release x64
    runs-on: windows-2025-vs2026

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
        with:
          vs-version: '[18.0,19.0)'
          vs-prerelease: true
          msbuild-architecture: x64

      - name: Show MSBuild version
        run: msbuild -version

      - name: Build
        run: >
          msbuild Shingiittai.slnx
          /m
          /p:Configuration=Release
          /p:Platform=x64
          /p:PlatformToolset=v145
          /p:VisualStudioVersion=18.0
          /p:WindowsTargetPlatformVersion=10.0

      - name: Verify runtime output
        shell: pwsh
        run: |
          $outputDir = Join-Path $env:GITHUB_WORKSPACE "..\generated\outputs\Release\App"
          $requiredFiles = @(
            "App.exe",
            "JoyShockLibrary.dll",
            "assimp-vc145-mt.dll",
            "app\resources\title\title_logo.png",
            "engine\resources\shaders\sprite\SpriteVS.hlsl"
          )

          foreach ($file in $requiredFiles) {
            $path = Join-Path $outputDir $file
            if (-not (Test-Path $path)) {
              throw "Required output is missing: $path"
            }
          }

      - name: Stage artifact
        shell: pwsh
        run: |
          $outputDir = Join-Path $env:GITHUB_WORKSPACE "..\generated\outputs\Release\App"
          $distDir = Join-Path $env:GITHUB_WORKSPACE "dist\Shingiittai-Release-x64"
          if (Test-Path $distDir) {
            Remove-Item $distDir -Recurse -Force
          }
          New-Item -ItemType Directory -Path $distDir | Out-Null
          Copy-Item -Path (Join-Path $outputDir "*") -Destination $distDir -Recurse -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Shingiittai-Release-x64
          path: |
            dist/Shingiittai-Release-x64/**
            !dist/Shingiittai-Release-x64/*.ilk
            !dist/Shingiittai-Release-x64/*.lib
            !dist/Shingiittai-Release-x64/*.pdb

  publish:
    name: Publish GitHub Release
    runs-on: windows-2025-vs2026
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download Release artifact
        uses: actions/download-artifact@v4
        with:
          name: Shingiittai-Release-x64
          path: dist/Shingiittai

      - name: Package
        shell: pwsh
        run: |
          Compress-Archive -Path dist/Shingiittai/* -DestinationPath Shingiittai-${{ github.ref_name }}-x64.zip -Force

      - name: Create GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" `
            "Shingiittai-${{ github.ref_name }}-x64.zip" `
            --title "Shingiittai ${{ github.ref_name }}" `
            --notes "Automated Release build for ${{ github.ref_name }}."
